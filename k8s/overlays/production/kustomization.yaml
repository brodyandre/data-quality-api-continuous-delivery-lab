apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: data-quality-production
nameSuffix: -production

resources:
  - ../../base/deployment.yaml
  - ../../base/service.yaml
  - configmap.yaml

commonLabels:
  app.kubernetes.io/environment: production
